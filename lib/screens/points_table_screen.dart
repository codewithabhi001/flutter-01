import 'package:flutter/material.dart';

class PointsTableScreen extends StatelessWidget {
  const PointsTableScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Points Table Screen'),
      ),
    );
  }
}
